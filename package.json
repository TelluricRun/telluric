{
  "name": "telluric",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "clean": "rm -rf **/.next **/.turbo node_modules"
  },
  "devDependencies": {
    "turbo": "latest"
  }
}