{
  "name": "telluric",
  "private": true,
  "version": "0.1.0",
  "packageManager": "npm@10.8.2",
  "workspaces": [
	"apps/*",
	"web/*",
	"packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "clean": "rm -rf **/.next **/.turbo node_modules"
  },
  "devDependencies": {
    "turbo": "latest"
  }
}